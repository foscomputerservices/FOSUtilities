// Button+AsyncAction.swift
//
// Copyright 2026 FOS Computer Services, LLC
//
// Licensed under the Apache License, Version 2.0 (the  License);
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// GENERATED FILE — DO NOT EDIT
// Generated by scripts/localizable-overload-sweep.swift (Stage 6b — FOS-designed async twins)
// SDKs: macosx 26.5 | iphoneos 26.5 | appletvos 26.5 | watchos 26.5 | xros 26.5
// Regenerate: swift scripts/localizable-overload-sweep.swift [--emit-async-only]

#if canImport(SwiftUI)
import DeveloperToolsSupport
import SwiftUI

public extension Button where Label == SwiftUI.Label<Text, Image> {
    /// Async form of the `Localizable` Button — runs a throwing async action and routes
    /// its error to a binding
    ///
    /// ## Example
    ///
    /// ```swift
    /// @ViewModel public struct MyViewModel: RequestableViewModel {
    ///     @LocalizedString public var saveTitle
    ///     ...
    /// }
    ///
    /// @State private var error: Error?
    ///
    /// Button(viewModel.saveTitle, systemImage: "tray.and.arrow.down", error: $error) {
    ///     try await viewModel.operations.save()
    /// }
    /// .alert(error: $error,
    ///        title: viewModel.errorTitle,
    ///        dismissButtonLabel: viewModel.dismissTitle)
    /// ```
    ///
    /// Tapping starts the action; a thrown error lands in `error`, and every launch
    /// clears it first — the binding holds the outcome of the most recent invocation.
    /// Pass `activity:` to refuse taps while a run is in flight; without it every tap
    /// starts a new concurrent invocation. The task runs to completion — for
    /// user-cancellable work use the `cancelTitle:` forms.
    ///
    /// - Parameters:
    ///   - localizable: The ``Localizable`` title to display.
    ///   - defaultValue: Fallback text used if localization did not complete.
    ///   - activity: Optional caller-owned ``AsyncButtonActivity`` enabling re-entry
    ///     refusal and running-state display.
    ///   - error: Receives the outcome of the most recent invocation.
    nonisolated init(_ localizable: some Localizable, defaultValue: String? = nil, systemImage: String, role: ButtonRole?, activity: Binding<AsyncButtonActivity>? = nil, error: Binding<Error?>, action: @escaping @Sendable () async throws -> Void) {
        self.init(role: role, activity: activity, error: error, action: action, label: {
            SwiftUI.Label(localizable.defaultedLocalizedString(defaultValue: defaultValue), systemImage: systemImage)
        })
    }

    /// A two-faced async button: tap to start the operation, tap again to cancel it
    ///
    /// While idle the button shows `localizable`; while running it shows `cancelTitle` and
    /// a tap cancels the operation. During the unwind (`activity.phase == .cancelling`)
    /// taps are refused. A cancelled invocation writes nothing to `error`; a failed one
    /// deposits its error there, and every launch clears it first.
    ///
    /// > Important: Cancellation is cooperative — the action must run cancellation-aware
    /// > work (any `URLSession`-backed `ServerRequest` is) for the cancel face to take
    /// > effect.
    ///
    /// - Parameters:
    ///   - localizable: The ``Localizable`` idle-face title.
    ///   - defaultValue: Fallback text used if localization did not complete.
    ///   - cancelTitle: The ``Localizable`` title shown while the operation runs.
    ///   - cancelDefaultValue: Fallback text for `cancelTitle`.
    ///   - cancelSystemImage: The running-face symbol; `nil` keeps `systemImage`.
    ///   - activity: The caller-owned ``AsyncButtonActivity`` (required — cancellation
    ///     needs state that survives re-renders).
    ///   - error: Receives the outcome of the most recent invocation.
    nonisolated init(_ localizable: some Localizable, defaultValue: String? = nil, systemImage: String, cancelTitle: some Localizable, cancelDefaultValue: String? = nil, cancelSystemImage: String? = nil, role: ButtonRole?, activity: Binding<AsyncButtonActivity>, error: Binding<Error?>, action: @escaping @Sendable () async throws -> Void) {
        self.init(role: role, activity: activity, error: error, action: action, label: { phase in
            SwiftUI.Label(
                phase == .idle ? localizable.defaultedLocalizedString(defaultValue: defaultValue) : cancelTitle.defaultedLocalizedString(defaultValue: cancelDefaultValue),
                systemImage: phase == .idle ? systemImage : (cancelSystemImage ?? systemImage)
            )
        })
    }

    /// Async form of the `Localizable` Button — runs a throwing async action and routes
    /// its error to a binding
    ///
    /// Tapping starts the action; a thrown error lands in `error`, and every launch
    /// clears it first — the binding holds the outcome of the most recent invocation.
    /// Pass `activity:` to refuse taps while a run is in flight; without it every tap
    /// starts a new concurrent invocation. The task runs to completion — for
    /// user-cancellable work use the `cancelTitle:` forms.
    ///
    /// - Parameters:
    ///   - localizable: The ``Localizable`` title to display.
    ///   - defaultValue: Fallback text used if localization did not complete.
    ///   - activity: Optional caller-owned ``AsyncButtonActivity`` enabling re-entry
    ///     refusal and running-state display.
    ///   - error: Receives the outcome of the most recent invocation.
    nonisolated init(_ localizable: some Localizable, defaultValue: String? = nil, systemImage: String, activity: Binding<AsyncButtonActivity>? = nil, error: Binding<Error?>, action: @escaping @Sendable () async throws -> Void) {
        self.init(activity: activity, error: error, action: action, label: {
            SwiftUI.Label(localizable.defaultedLocalizedString(defaultValue: defaultValue), systemImage: systemImage)
        })
    }

    /// A two-faced async button: tap to start the operation, tap again to cancel it
    ///
    /// While idle the button shows `localizable`; while running it shows `cancelTitle` and
    /// a tap cancels the operation. During the unwind (`activity.phase == .cancelling`)
    /// taps are refused. A cancelled invocation writes nothing to `error`; a failed one
    /// deposits its error there, and every launch clears it first.
    ///
    /// > Important: Cancellation is cooperative — the action must run cancellation-aware
    /// > work (any `URLSession`-backed `ServerRequest` is) for the cancel face to take
    /// > effect.
    ///
    /// - Parameters:
    ///   - localizable: The ``Localizable`` idle-face title.
    ///   - defaultValue: Fallback text used if localization did not complete.
    ///   - cancelTitle: The ``Localizable`` title shown while the operation runs.
    ///   - cancelDefaultValue: Fallback text for `cancelTitle`.
    ///   - cancelSystemImage: The running-face symbol; `nil` keeps `systemImage`.
    ///   - activity: The caller-owned ``AsyncButtonActivity`` (required — cancellation
    ///     needs state that survives re-renders).
    ///   - error: Receives the outcome of the most recent invocation.
    nonisolated init(_ localizable: some Localizable, defaultValue: String? = nil, systemImage: String, cancelTitle: some Localizable, cancelDefaultValue: String? = nil, cancelSystemImage: String? = nil, activity: Binding<AsyncButtonActivity>, error: Binding<Error?>, action: @escaping @Sendable () async throws -> Void) {
        self.init(activity: activity, error: error, action: action, label: { phase in
            SwiftUI.Label(
                phase == .idle ? localizable.defaultedLocalizedString(defaultValue: defaultValue) : cancelTitle.defaultedLocalizedString(defaultValue: cancelDefaultValue),
                systemImage: phase == .idle ? systemImage : (cancelSystemImage ?? systemImage)
            )
        })
    }

    /// Async form of the `Localizable` Button — runs a throwing async action and routes
    /// its error to a binding
    ///
    /// Tapping starts the action; a thrown error lands in `error`, and every launch
    /// clears it first — the binding holds the outcome of the most recent invocation.
    /// Pass `activity:` to refuse taps while a run is in flight; without it every tap
    /// starts a new concurrent invocation. The task runs to completion — for
    /// user-cancellable work use the `cancelTitle:` forms.
    ///
    /// - Parameters:
    ///   - localizable: The ``Localizable`` title to display.
    ///   - defaultValue: Fallback text used if localization did not complete.
    ///   - activity: Optional caller-owned ``AsyncButtonActivity`` enabling re-entry
    ///     refusal and running-state display.
    ///   - error: Receives the outcome of the most recent invocation.
    nonisolated init(_ localizable: some Localizable, defaultValue: String? = nil, image: ImageResource, role: ButtonRole?, activity: Binding<AsyncButtonActivity>? = nil, error: Binding<Error?>, action: @escaping @Sendable () async throws -> Void) {
        self.init(role: role, activity: activity, error: error, action: action, label: {
            SwiftUI.Label(localizable.defaultedLocalizedString(defaultValue: defaultValue), image: image)
        })
    }

    /// A two-faced async button: tap to start the operation, tap again to cancel it
    ///
    /// While idle the button shows `localizable`; while running it shows `cancelTitle` and
    /// a tap cancels the operation. During the unwind (`activity.phase == .cancelling`)
    /// taps are refused. A cancelled invocation writes nothing to `error`; a failed one
    /// deposits its error there, and every launch clears it first.
    ///
    /// > Important: Cancellation is cooperative — the action must run cancellation-aware
    /// > work (any `URLSession`-backed `ServerRequest` is) for the cancel face to take
    /// > effect.
    ///
    /// - Parameters:
    ///   - localizable: The ``Localizable`` idle-face title.
    ///   - defaultValue: Fallback text used if localization did not complete.
    ///   - cancelTitle: The ``Localizable`` title shown while the operation runs.
    ///   - cancelDefaultValue: Fallback text for `cancelTitle`.
    ///   - cancelImage: The running-face image; `nil` keeps `image`.
    ///   - activity: The caller-owned ``AsyncButtonActivity`` (required — cancellation
    ///     needs state that survives re-renders).
    ///   - error: Receives the outcome of the most recent invocation.
    nonisolated init(_ localizable: some Localizable, defaultValue: String? = nil, image: ImageResource, cancelTitle: some Localizable, cancelDefaultValue: String? = nil, cancelImage: ImageResource? = nil, role: ButtonRole?, activity: Binding<AsyncButtonActivity>, error: Binding<Error?>, action: @escaping @Sendable () async throws -> Void) {
        self.init(role: role, activity: activity, error: error, action: action, label: { phase in
            SwiftUI.Label(
                phase == .idle ? localizable.defaultedLocalizedString(defaultValue: defaultValue) : cancelTitle.defaultedLocalizedString(defaultValue: cancelDefaultValue),
                image: phase == .idle ? image : (cancelImage ?? image)
            )
        })
    }

    /// Async form of the `Localizable` Button — runs a throwing async action and routes
    /// its error to a binding
    ///
    /// Tapping starts the action; a thrown error lands in `error`, and every launch
    /// clears it first — the binding holds the outcome of the most recent invocation.
    /// Pass `activity:` to refuse taps while a run is in flight; without it every tap
    /// starts a new concurrent invocation. The task runs to completion — for
    /// user-cancellable work use the `cancelTitle:` forms.
    ///
    /// - Parameters:
    ///   - localizable: The ``Localizable`` title to display.
    ///   - defaultValue: Fallback text used if localization did not complete.
    ///   - activity: Optional caller-owned ``AsyncButtonActivity`` enabling re-entry
    ///     refusal and running-state display.
    ///   - error: Receives the outcome of the most recent invocation.
    nonisolated init(_ localizable: some Localizable, defaultValue: String? = nil, image: ImageResource, activity: Binding<AsyncButtonActivity>? = nil, error: Binding<Error?>, action: @escaping @Sendable () async throws -> Void) {
        self.init(activity: activity, error: error, action: action, label: {
            SwiftUI.Label(localizable.defaultedLocalizedString(defaultValue: defaultValue), image: image)
        })
    }

    /// A two-faced async button: tap to start the operation, tap again to cancel it
    ///
    /// While idle the button shows `localizable`; while running it shows `cancelTitle` and
    /// a tap cancels the operation. During the unwind (`activity.phase == .cancelling`)
    /// taps are refused. A cancelled invocation writes nothing to `error`; a failed one
    /// deposits its error there, and every launch clears it first.
    ///
    /// > Important: Cancellation is cooperative — the action must run cancellation-aware
    /// > work (any `URLSession`-backed `ServerRequest` is) for the cancel face to take
    /// > effect.
    ///
    /// - Parameters:
    ///   - localizable: The ``Localizable`` idle-face title.
    ///   - defaultValue: Fallback text used if localization did not complete.
    ///   - cancelTitle: The ``Localizable`` title shown while the operation runs.
    ///   - cancelDefaultValue: Fallback text for `cancelTitle`.
    ///   - cancelImage: The running-face image; `nil` keeps `image`.
    ///   - activity: The caller-owned ``AsyncButtonActivity`` (required — cancellation
    ///     needs state that survives re-renders).
    ///   - error: Receives the outcome of the most recent invocation.
    nonisolated init(_ localizable: some Localizable, defaultValue: String? = nil, image: ImageResource, cancelTitle: some Localizable, cancelDefaultValue: String? = nil, cancelImage: ImageResource? = nil, activity: Binding<AsyncButtonActivity>, error: Binding<Error?>, action: @escaping @Sendable () async throws -> Void) {
        self.init(activity: activity, error: error, action: action, label: { phase in
            SwiftUI.Label(
                phase == .idle ? localizable.defaultedLocalizedString(defaultValue: defaultValue) : cancelTitle.defaultedLocalizedString(defaultValue: cancelDefaultValue),
                image: phase == .idle ? image : (cancelImage ?? image)
            )
        })
    }
}

public extension Button where Label == Text {
    /// Async form of the `Localizable` Button — runs a throwing async action and routes
    /// its error to a binding
    ///
    /// Tapping starts the action; a thrown error lands in `error`, and every launch
    /// clears it first — the binding holds the outcome of the most recent invocation.
    /// Pass `activity:` to refuse taps while a run is in flight; without it every tap
    /// starts a new concurrent invocation. The task runs to completion — for
    /// user-cancellable work use the `cancelTitle:` forms.
    ///
    /// - Parameters:
    ///   - localizable: The ``Localizable`` title to display.
    ///   - defaultValue: Fallback text used if localization did not complete.
    ///   - activity: Optional caller-owned ``AsyncButtonActivity`` enabling re-entry
    ///     refusal and running-state display.
    ///   - error: Receives the outcome of the most recent invocation.
    nonisolated init(_ localizable: some Localizable, defaultValue: String? = nil, role: ButtonRole?, activity: Binding<AsyncButtonActivity>? = nil, error: Binding<Error?>, action: @escaping @Sendable () async throws -> Void) {
        self.init(role: role, activity: activity, error: error, action: action, label: {
            Text(localizable.defaultedLocalizedString(defaultValue: defaultValue))
        })
    }

    /// A two-faced async button: tap to start the operation, tap again to cancel it
    ///
    /// While idle the button shows `localizable`; while running it shows `cancelTitle` and
    /// a tap cancels the operation. During the unwind (`activity.phase == .cancelling`)
    /// taps are refused. A cancelled invocation writes nothing to `error`; a failed one
    /// deposits its error there, and every launch clears it first.
    ///
    /// > Important: Cancellation is cooperative — the action must run cancellation-aware
    /// > work (any `URLSession`-backed `ServerRequest` is) for the cancel face to take
    /// > effect.
    ///
    /// - Parameters:
    ///   - localizable: The ``Localizable`` idle-face title.
    ///   - defaultValue: Fallback text used if localization did not complete.
    ///   - cancelTitle: The ``Localizable`` title shown while the operation runs.
    ///   - cancelDefaultValue: Fallback text for `cancelTitle`.
    ///   - activity: The caller-owned ``AsyncButtonActivity`` (required — cancellation
    ///     needs state that survives re-renders).
    ///   - error: Receives the outcome of the most recent invocation.
    nonisolated init(_ localizable: some Localizable, defaultValue: String? = nil, cancelTitle: some Localizable, cancelDefaultValue: String? = nil, role: ButtonRole?, activity: Binding<AsyncButtonActivity>, error: Binding<Error?>, action: @escaping @Sendable () async throws -> Void) {
        self.init(role: role, activity: activity, error: error, action: action, label: { phase in
            Text(phase == .idle ? localizable.defaultedLocalizedString(defaultValue: defaultValue) : cancelTitle.defaultedLocalizedString(defaultValue: cancelDefaultValue))
        })
    }

    /// Async form of the `Localizable` Button — runs a throwing async action and routes
    /// its error to a binding
    ///
    /// Tapping starts the action; a thrown error lands in `error`, and every launch
    /// clears it first — the binding holds the outcome of the most recent invocation.
    /// Pass `activity:` to refuse taps while a run is in flight; without it every tap
    /// starts a new concurrent invocation. The task runs to completion — for
    /// user-cancellable work use the `cancelTitle:` forms.
    ///
    /// - Parameters:
    ///   - localizable: The ``Localizable`` title to display.
    ///   - defaultValue: Fallback text used if localization did not complete.
    ///   - activity: Optional caller-owned ``AsyncButtonActivity`` enabling re-entry
    ///     refusal and running-state display.
    ///   - error: Receives the outcome of the most recent invocation.
    nonisolated init(_ localizable: some Localizable, defaultValue: String? = nil, activity: Binding<AsyncButtonActivity>? = nil, error: Binding<Error?>, action: @escaping @Sendable () async throws -> Void) {
        self.init(activity: activity, error: error, action: action, label: {
            Text(localizable.defaultedLocalizedString(defaultValue: defaultValue))
        })
    }

    /// A two-faced async button: tap to start the operation, tap again to cancel it
    ///
    /// While idle the button shows `localizable`; while running it shows `cancelTitle` and
    /// a tap cancels the operation. During the unwind (`activity.phase == .cancelling`)
    /// taps are refused. A cancelled invocation writes nothing to `error`; a failed one
    /// deposits its error there, and every launch clears it first.
    ///
    /// > Important: Cancellation is cooperative — the action must run cancellation-aware
    /// > work (any `URLSession`-backed `ServerRequest` is) for the cancel face to take
    /// > effect.
    ///
    /// - Parameters:
    ///   - localizable: The ``Localizable`` idle-face title.
    ///   - defaultValue: Fallback text used if localization did not complete.
    ///   - cancelTitle: The ``Localizable`` title shown while the operation runs.
    ///   - cancelDefaultValue: Fallback text for `cancelTitle`.
    ///   - activity: The caller-owned ``AsyncButtonActivity`` (required — cancellation
    ///     needs state that survives re-renders).
    ///   - error: Receives the outcome of the most recent invocation.
    nonisolated init(_ localizable: some Localizable, defaultValue: String? = nil, cancelTitle: some Localizable, cancelDefaultValue: String? = nil, activity: Binding<AsyncButtonActivity>, error: Binding<Error?>, action: @escaping @Sendable () async throws -> Void) {
        self.init(activity: activity, error: error, action: action, label: { phase in
            Text(phase == .idle ? localizable.defaultedLocalizedString(defaultValue: defaultValue) : cancelTitle.defaultedLocalizedString(defaultValue: cancelDefaultValue))
        })
    }
}
#endif
